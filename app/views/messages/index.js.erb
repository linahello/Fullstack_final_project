var email = document.getElementById("email");
email.innerHTML = "<%= j render 'email_section', conversation: @conversation %>";
var div = document.getElementById("message_box");
div.scrollTop = div.scrollHeight;


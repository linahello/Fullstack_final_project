<div class="container h-100 py-2">
  <div class="mb-4">
    <% if current_user.is_artist %>
      <%= link_to "Retour à la liste", venue_profiles_path %>
    <% else %>
      <%= link_to "Retour dashboard", dashboard_path(current_user.id) %>
    <% end %>
  </div>
  <div class="container mt-4 card p-0 mb-5">
    <div class="event_header d-flex flex-row flex-wrap">
      <div class="col-md-8 p-0">
        <% if @venue_profile.venuepict.attached? %>
          <%= image_tag @venue_profile.venuepict, alt: "venue picture", style: "width: 100%; border-top-left-radius: 8px"%>
        <% else %>
          <p>🖼  Il n'y a pas encore d'image pour ce profil</p>
        <% end %>
      </div>
      <div class="col-md-4 p-3 d-flex flex-column align-items-center justify-content-center">
        <h1 class="mb-4"><%= @venue_profile.name %></h1>
        <h3><%= @venue_profile.address%></h3>
        <h4><%= @venue_profile.zipcode%>, <%= @venue_profile.city%></h4>
      </div>
    </div>
    <div class=" d-flex flex-row flex-wrap" style="border-bottom: 1px solid rgba(255, 255, 255, 0.231); border-top: 1px solid rgba(255, 255, 255, 0.231)" >
      <div class="col-lg-8 col-md-4 py-3 ps-5 align-items-center d-none d-sm-block">
      </div>
      <div class="col-lg-4 col-md-8 p-3">
        <% if current_user.id == @venue_profile.user_id %>
          <%= link_to "Modifier mon profil", edit_venue_profile_path, class:"btn btn-lg btn-primary" %>
        <% elsif current_user.is_artist? %>
          <%= link_to "Contacter", edit_venue_profile_path, class:"btn btn-lg btn-primary" %>
        <% end %>
      </div>
    </div>
    <div class="row" >
      <div class="col-md-8 p-4">
        <h3>A propos</h3>
        <p><%= @venue_profile.description%></p>
      </div>
      <div class="col-md-4 p-3 ">
        <p>Type: <%= @venue_profile.type_of_location%></p>
        <p>Capacité d'accueil: <%= @venue_profile.capacity%> personnes</p>
        <p>contact: <%= @venue_profile.user.email%></p>
      </div>
    </div>
  </div>
</div>
